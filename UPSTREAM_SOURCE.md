# Upstream source

Requested source repository:

https://github.com/vn0raven/cargo_panic_oop

The execution environment could inspect the public repository but could not complete a direct `git clone` or archive transfer. This deliverable is therefore a clean-room UI-enhanced fork of the documented sorting loop rather than a byte-for-byte checkout with a patch applied.
